"use strict";

app.component("aktivität", {
    templateUrl: "components/aktivität.html",
    controller: "aktivitätController",
    bindings: {
    }
});

app.controller("aktivitätController", function () {
    let $ctrl = this;

});